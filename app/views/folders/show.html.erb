

<%@folder.photos_as_thumbnail.each do |photo|%>

    <%=  image_tag photo%>

    <%= button_to 'Delete',delete_photo_attachment_folder_path(photo.blob.id), 
                            method: :delete
                            %>
<%end%>
<br><br><br>
<%= link_to "Back", root_path %><br>
</form>
